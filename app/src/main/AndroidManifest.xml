<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.daher928.bmw_display_system">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme"
        tools:replace="android:supportsRtl">
        <activity
            android:name=".MainMenu"
            android:screenOrientation="landscape">
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".ChooseCond"
            android:screenOrientation="landscape" />
        <activity android:name=".ChooseGrid" />
        <activity android:name=".ChooseConfig" />
        <activity android:name=".AboutHelp" />
        <activity android:name=".PreviousConfigs" />
        <activity android:name=".Options" />
        <activity android:name=".LogStats"/>
        <activity
            android:name=".authentication.AuthenticationLogin"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".authentication.AuthenticationMain"/>
        <activity android:name=".authentication.AuthenticationUser"/>
        <activity android:name=".LiveConditions" />

    </application>

</manifest>